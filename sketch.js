function setup() {
  let canvas = createCanvas(windowWidth, windowHeight);
  canvas.parent('myContainer')
}

function draw() {
  background(75, 100 , 180);
  // noStroke()
  // rect(40, 40, 1400, 50, 10, 10, 10, 10)
}
